const adminSchema = require('./admin');
const bankSchema = require('./bank');
const currencySchema = require('./currency');
const regionSchema = require('./region');
const areaparentSchema = require('./areaparent');
const storeSchema =  require('./store')

module.exports = {
    adminSchema,
    bankSchema,
    currencySchema,
    regionSchema,
    areaparentSchema,
    storeSchema
}